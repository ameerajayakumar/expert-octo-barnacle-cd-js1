const metadata = {
  title: 'Scratchpad',
  locale: 'de',
  url: '/en-US/docs/Tools/Scratchpad',
};

const metadataCopy = { ...metadata };
metadataCopy.name = 'scratchpad';

const { title, locale, url, name } = metadataCopy;
/**
 * 
 * Given above object
Create a clone of the object, metadataCopy
Add a new property “name” with your name to metadataCopy object (this shouldn’t alter the original object)
Store the metadataCopy property values to a variable each of the same name (title, locale, url, name) using Spread syntax

 */
